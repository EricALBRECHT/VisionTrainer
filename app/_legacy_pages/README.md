"""Anciennes pages multipages Streamlit.

Conservées pour référence. La navigation active est définie dans
``app/streamlit_app.py`` via ``st.navigation`` / ``st.Page``.
Les corps UI sont dans ``app/views/``.
"""
